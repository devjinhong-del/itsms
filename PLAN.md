# PLAN

## 이번 사이클 목표
PRD.md에 정의된 ITSMS 기능(데이터 적재, 자산 실사, 역할별 대시보드, 라이선스 현황, AI 챗봇)을 실제 사내 데이터가 도는 상태까지 만들고 배포한다.

원래 계획은 "로그인은 맨 마지막(19번)"이었으나, 실데이터를 다루려면 접근 제어가 먼저 있어야 해서 **로그인을 앞당겨 구현한 뒤 실데이터로 전환**했다.

## 성공 기준
- 각 단계마다 `npm run lint` / `npm test` / `npm run build`가 오류 없이 통과한다. → **달성** (GitHub Actions CI가 push마다 자동 확인)
- 화면이 있는 작업은 실제로 열어 PRD 4번(사용자와 이용 흐름)대로 보이는지 확인한다. → **달성**
- 외부 API 키·인증 정보는 `.env`에서만 읽고 코드·화면·로그에 노출되지 않는다. → **달성** (개인정보도 코드에서 분리, `.env.example` 참고)
- PRD 6번(비범위) 항목은 만들지 않는다. → **지킴** (라이선스 실제 할당/해지, 승인 없는 자동 확정 없음)

## 현재 상태 한눈에

| 구분 | 개수 |
|---|---|
| 완료 | 12 |
| 범위 조정(다음 사이클) | 7 |

실제 운영 중인 데이터: 자산 **1,010대** · 로그인 계정 **1,097개** · 챗봇 학습 자료 **265조각**

## 작업 순서와 결과

| # | 작업 | 상태 | 결과물 |
|---|---|---|---|
| 1 | Next.js 라우팅·페이지 구조 | ✅ 완료 | `src/app` (App Router, 라우트 그룹 `(main)`) |
| 2 | Supabase 표 만들기 | ✅ 완료 | `db/migrations/001~009` — 계획했던 `users`는 `profiles`로 대체 |
| 3 | M365 사용자 CSV 적재 | ✅ 완료 | `export_m365/sync_m365.py` → `m365_users` (화면 업로드 UI 대신 배치로) |
| 4 | 렌탈사이트(KRS) 자산 적재 | ✅ 완료 | `export_OA/sync.py` → `krs_*` 5개 표 |
| 5 | 개인 현황 대시보드 | ✅ 완료 | `src/app/(main)/page.tsx` — 내 장비·라이선스·실사 상태 |
| 6 | 담당자용 조직 현황 | ✅ 완료 | `src/app/(main)/org-assets/page.tsx` — 담당자(manager) 이상만 접근 |
| 7 | 바코드 실사 입력 화면 | ✅ 완료 | `src/app/audit_oa/` — `@zxing/browser`, 실사 사진 촬영 포함 |
| 8 | 실사 결과 제출 | ✅ 완료 | `src/app/audit_oa/actions.ts`의 `submitAudit` → `audit_oa` |
| 9 | 정보 수정 후 변경 제출 | ✅ 완료 | 수정 여부(`is_edited`) 기록, SCD2로 이전 기록 만료 |
| 10 | 실사 현황 모니터링 | ✅ 완료 | `src/app/(main)/admin/audit-monitoring/page.tsx` — 진행률·부서별 순위·실사 사진 |
| 14 | 라이선스 수량 관리 | ✅ 완료 | `src/app/(main)/admin/license/page.tsx` — 구독 대비 사용률, License × 조직 |
| 17 | AI 챗봇 | ✅ 완료 | RAG로 구현 — `src/lib/rag/`, 학습 관리 화면 `admin/rag` |
| 19 | 로그인·권한 제어 | ✅ 완료 | Supabase Auth + `middleware.ts` + 역할 3단계(`src/lib/auth/role.ts`) |

### 계획에 없었지만 추가로 만든 것

| 작업 | 결과물 | 왜 |
|---|---|---|
| 전사 OA 현황 | `admin/oa-overview` | 관리자가 전체 분포를 먼저 보고 싶어 함 |
| OA Report & Insight | `admin/oa-status` | 단순 현황이 아니라 **회수 대상과 절감액**을 제시 — 퇴사자 명의 47대 / 연 1,327만 원 발굴 |
| 사용자 접속 로그 | `admin/access-log` | 누가 어떤 화면을 보는지 기록(감사 추적) |
| 실사 QR 안내 화면 | `admin/audit-qr` | 현장에서 휴대폰으로 바로 들어오게 |
| M365 라이선스 구독 수량 적재 | `m365_license_total` + `export_m365/sync_m365_license.py` | 사용률을 계산하려면 "구매 수량"이 필요 |

### 다음 사이클로 미룬 것

| # | 작업 | 이유 |
|---|---|---|
| 11 | 실사 일정 오픈/마감 관리 | 화면 자리는 잡아 둠(`admin/audit-open-close`). 첫 전사 실사를 돌려 본 뒤 일정 규칙을 정하기로 함 |
| 12·13 | 라이선스 신청·승인 흐름 | 신청을 받으려면 승인 주체·결재선이 먼저 정해져야 함. 현재는 **현황 조회까지** 제공 |
| 15 | 비밀번호 변경 화면 | Supabase Auth 기능이라 화면만 붙이면 되는 작업. 우선순위 뒤로 |
| 16 | 담당자 선임 | 화면 자리는 잡아 둠(`admin/assignee`). 역할 체계를 먼저 정리해야 해서 뒤로 |
| 18 | 챗봇 HR 요청 자동화 | 메일 발송 연동이 필요. PRD 비범위(자동 확정 금지)와 맞물려 신중히 |

## 다음에 할 일
1. 전사 실사 오픈 → 완료율 100% 달성(성공 기준)
2. 실사 결과로 자산 대장 정정 절차 만들기
3. 라이선스 신청·승인 흐름 연결(12·13)
4. 챗봇 질문 로그를 남겨 "무엇을 더 학습시켜야 하는지" 파악
