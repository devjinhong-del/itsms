# PLAN

## 이번 사이클 목표
PRD.md에 정의된 ITSMS 기능 전체(데이터 적재, 자산 실사, 역할별 대시보드, 라이선스 관리, AI 챗봇)를, 로그인이 붙기 전까지는 더미 데이터로 화면·흐름을 먼저 완성하고 마지막에 로그인을 붙이는 순서로 만든다.

## 성공 기준
- 아래 작업 19개가 모두 구현되고, 각 단계마다 `npm run lint` / `npm run build`가 오류 없이 통과한다.
- 화면이 있는 작업은 `npm run dev`로 실제로 열어 확인했을 때 PRD.md 4번(사용자와 이용 흐름)에 적은 대로 보인다.
- 외부 API 키·인증 정보(Supabase, 렌탈사이트, Anthropic 등)는 `.env`에서만 읽고, 코드나 화면·로그에 노출되지 않는다.
- PRD.md 6번(비범위)에 적은 항목(실제 라이선스 할당/해지 자동 실행, 승인 없는 자동 확정 처리)은 만들지 않는다.
- 작업 19(로그인)가 붙기 전까지는 실제 임직원 개인정보 대신 더미 데이터만 사용한다(DESIGN.md 0번 참고).

## 작업 순서
1. Next.js 기본 라우팅/페이지 구조 만들기 (로그인 없이, 더미 데이터로 화면 뼈대 먼저 확인)
2. Supabase 프로젝트를 만들고 DESIGN.md의 데이터 모델대로 표(users/equipment/inspections/license_requests/inspection_schedules/assignees)를 만들기
3. ~~M365 PowerShell로 내보낸 사용자 CSV를 업로드해서 저장하는 화면·API 만들기~~ **완료(스크립트로)** — `export_m365/*.csv`를 `npm run sync:m365`(`scripts/sync-m365.ts`)로 파싱해 `m365_users` 테이블에 적재. 화면 업로드 UI는 아직 없고, 이 로직을 그대로 API 라우트로 감싸면 됨
4. 렌탈사이트(KRS) API로 OA 자산 데이터 가져와 저장하기 — `export_OA` Python 수집기로 구현 완료(KRS 조회 API 5개 → 테이블 5개, 변경분만 이력으로 쌓는 방식). 현재 벤더 측 권한 문제로 실데이터 적재만 대기 중
5. 저장된 데이터로 개인 현황 대시보드 화면 만들기 (이름/사진/이메일/장비/실사·라이선스 현황)
6. 담당자용 조직 현황 화면 만들기 (팀/실 구성원 목록)
7. ~~카메라로 바코드를 스캔해 자산번호·실사용자 정보를 인식하는 실사 입력 화면 만들기~~ **완료** — `/audit_oa` (`src/app/audit_oa/`), `@zxing/browser`로 바코드 인식
8. ~~실사 결과 제출 API 만들기 (자산번호 + 실사용자 정보 저장)~~ **완료** — `src/app/audit_oa/actions.ts`의 `submitAudit`, `audit_oa` 테이블에 저장
9. ~~실사용자 정보가 틀렸을 때 수정 후 "변경 제출"하는 흐름 추가하기~~ **완료** — 텍스트필드 수정 + 편집여부(`is_edited`) 기록, SCD2로 이전 기록 만료 처리
10. 관리자용 실사 현황 모니터링 화면 만들기 (진행률, 조직별·개인별 현황)
11. 실사 일정 오픈/마감 관리 화면 만들기 (생성·변경·마감·취소)
12. 라이선스 보유 리스트 조회 및 신청 화면 만들기
13. 라이선스 신청 승인/거절 처리 화면 만들기 (관리자가 확인 후 확정)
14. 라이선스 수량 관리 화면 만들기 (전체 대비 사용량, 팀별·종류별 현황)
15. 사용자 정보 바꾸기(비밀번호 변경) 화면 만들기
16. 담당자 선임 화면 만들기 (서비스 영역별 담당자 지정/변경)
17. AI 챗봇 기본 응답 만들기 (담당자·시스템 담당자 연락처 확인, 더미 데이터 기반 최소 정보만 전달)
18. AI 챗봇에 HR 요청 알림 및 자동/수동 처리 흐름 연동하기 (메일 발송은 기록만 남기고 실제 전송은 이후 과제)
19. ~~로그인(이메일/비밀번호) 및 사용자 권한별 접근 제어를 추가하고, 이 시점부터 실제 데이터로 전환하기~~ **완료 — 배포 준비를 위해 순서를 앞당겨 먼저 구현함** (Supabase Auth, `profiles` 테이블, 미들웨어 라우트 보호, `krs_*` 테이블 RLS 잠금까지 포함)
