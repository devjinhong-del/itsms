# PRD

## 1. 추진 배경 (문제 정의)
*누가, 어떤 업무에서, 어떤 불편을 겪는가*

IT/전산 담당자가 사내 IT 서비스(OA 장비, 계정, 네트워크, 라이선스 등)를 안정적으로 운영·관리해야 하지만, 장비·계정·라이선스 정보가 렌탈사이트, M365 등 여러 시스템에 흩어져 있어 현황을 한눈에 파악하지 못하고, 자산 실사(누가 어떤 장비를 실제로 쓰고 있는지 확인)도 수작업에 의존하고 있어 업무 부담과 오류 가능성이 크다.

## 2. 현행 방식과 한계
*지금은 이 일을 어떻게 처리하고 있고, 무엇이 아쉬운가*

ITSMS(IT 서비스 관리)는 다음 구성요소로 나뉘어 각자 따로 관리되고 있다.

| 구성요소 | 현행 관리 방식 / 데이터 |
|---|---|
| OA 관리 | 장비별 실사용자 현황을 별도 확인 |
| 서버 관리 | 장비별 서비스, 장비별 계정 |
| 계정관리 | (서버/네트워크 관리와 분리되어 별도 운영) |
| 네트워크 관리 | 장비별 IP |
| 보안관리 | 서비스 요청서 기반 처리 |
| 변화관리 | 시스템 개선/변경 요청서, 데이터 변경 요청서 기반 처리 |
| 라이선스 관리 | (전체 사용 현황을 통합해서 보는 수단 없음) |
| 서비스별 담당자 | 영역별로 지정되어 있으나 한눈에 조회 불가 |

요청서(서비스 요청서, 시스템 개선/변경 요청서, 데이터 변경 요청서) 중심의 수작업 처리라서, 통합된 대시보드나 자동화된 데이터 수집 없이 담당자가 건건이 확인해야 하는 것이 가장 큰 한계다.

## 3. 목표와 기대효과 (성공 기준)
*측정 가능한 숫자로 (처리 시간, 문의 건수, 정답률 등)*

이번 프로젝트 목표는 다음 네 가지다.
1. OA·M365 데이터 쌓기 (여러 소스에 흩어진 데이터를 한 곳에 적재)
2. 실사하기 (OA 자산 현황을 정기적으로 확인)
3. AI 챗봇 (담당자 안내 및 반복 문의 자동 응대)
4. 있는 정보로 대시보드 추출 (쌓인 데이터를 역할별 화면으로 시각화)

> 성공 기준(구체 수치)은 아직 정해지지 않았다. 예시로 실사 완료율(%), 라이선스 신청 처리 소요 시간, AI 챗봇 문의 자동 해결 비율 등을 후보로 두고, 실제 목표 수치는 추진 배경 논의 후 별도로 채워 넣어야 한다.

## 4. 사용자와 이용 흐름
*누가 쓰는지와 사용 순서 (화살표로)*

사용자 유형: **일반 사용자 / 담당자 / 관리자** (+ 전 사용자 대상 **AI 챗봇**)

- 공통 진입: 로그인 → 메인화면(대시보드, 개인 현황: 이름·사진·이메일·관련 장비·실사 현황·실사 진행·사용 라이선스 현황)
- 일반 사용자: 대시보드 → 개인 현황 확인 → (필요 시) 사용자 정보 바꾸기(비밀번호 변경) / 라이선스 신청하기(보유 리스트 확인 → 신청 가능 여부 확인 → 신청)
- 담당자: 대시보드(개인 현황) → 조직 현황(팀/실 구성원 이름·장비·실사·라이선스 현황 조회)
- 관리자: 대시보드(OA 모든 현황) → 라이선스 수량 관리 / 실사 오픈·마감 관리(일정 생성·변경·마감·취소) / OA 자산 현황 실사 모니터링(실시간, 조직별·개인별) / 담당자 선임
- 전 사용자: AI 챗봇 → 담당자·시스템 담당자 연락처 확인, HR 요청(신규 입사자 라이선스 반영 등) 자동/수동 처리 및 알림

## 5. 주요 기능 (must / nice로 구분)
*must는 딱 2개까지, 기능마다 "AI가 지킬 규칙"을 함께*

**Must**
1. **OA·M365 데이터 통합 수집** — 렌탈사이트 API에서 장비 데이터, M365 PowerShell에서 사용자·이메일·소속·보유 라이선스·라이선스 사용 시작일을 가져와 적재한다. (Vercel 배포 환경에서는 PowerShell을 직접 실행할 수 없으므로, 담당자가 PowerShell로 내보낸 CSV 파일을 화면에서 업로드하는 방식으로 가져온다. 자세한 내용은 DESIGN.md 참고.)
   - AI가 지킬 규칙: 외부 API/PowerShell 인증 정보(키·자격증명)는 코드에 하드코딩하지 않고 환경변수(`.env`)로만 다루며, 절대 로그나 화면에 노출하지 않는다.
2. **OA 자산 현황 실사** — 로그인 후 카메라로 바코드를 자동 인식해 자산번호와 실사용자 정보를 가져오고, 정보가 틀리면 수정 후 제출(제출/변경 제출) 시 실사 완료 정보를 자산번호·실사용자 정보와 함께 전송한다.
   - AI가 지킬 규칙: 실사용자 정보가 일치하지 않을 때 임의로 자동 정정하지 않고, 반드시 사용자가 직접 확인·수정한 뒤 제출하게 한다.

**Nice**
- 역할별 대시보드 (일반 사용자 / 담당자 / 관리자 화면 분리)
- 라이선스 신청 및 수량 관리 (전체 대비 사용량, 팀별·종류별 현황)
- 실사 오픈/마감 관리 및 실시간 모니터링 (진행률, 팀/개인별 완료 현황)
- AI 챗봇 (담당자 연락처 안내, HR 라이선스 부족 알림 및 후속 처리 자동화/수동 처리 선택, 처리 완료 시 HR 메일 발송)
  - AI가 지킬 규칙: 사용자 권한(일반/담당자/관리자)별로 학습 데이터와 답변 범위를 분리해, 권한 밖의 개인정보나 조직 정보를 답변하지 않는다.

## 6. 범위와 비범위
*이번 실습에서 만들 것 / 절대 만들지 않을 것*

**만들 것**
- 렌탈사이트 API / M365 CSV 업로드를 통한 데이터 적재
- 바코드 기반 OA 자산 실사 입력 및 제출/변경 제출 흐름
- 일반 사용자·담당자·관리자 역할별 대시보드 및 메뉴, 담당자 조직 현황 조회
- 사용자 정보 바꾸기(비밀번호 변경), 담당자 선임
- 라이선스 신청/승인/수량 관리, 실사 오픈·마감·모니터링
- AI 챗봇(담당자 안내, HR 요청 알림 및 처리 상태 관리)

**절대 만들지 않을 것**
- M365 라이선스의 실제 할당/해지 등 외부 시스템에 대한 자동 실행(모두 조회·신청 요청까지만, 실제 처리는 담당자 수동 확인 후 진행)
- 실사·HR 요청 승인 없이 자동으로 확정되는 처리(자동화는 어디까지나 "제안/알림"이며 최종 확정은 사람이 한다)

## 7. 보안·개인정보 검토
*사용 문서의 공개등급, 개인정보 여부, API 키 관리 방법*

- 개인정보 포함: 이름, 사진, 이메일, 소속, 사용 장비, 라이선스 사용 현황 등 사내 임직원 개인정보가 다수 포함되므로 **사내 전용(대외비)** 등급으로 취급한다.
- 공개등급: 대시보드·실사 데이터는 로그인한 사내 사용자에게만 제공하며, 역할(일반/담당자/관리자)에 따라 조회 범위를 제한한다.
- **개발 단계 원칙**: 로그인·권한 제어(개발 단위 19번)가 붙기 전까지는 실제 임직원 개인정보 대신 더미(가짜) 데이터만 사용하고, Vercel에도 비공개(프리뷰) 상태로만 배포한다. 실제 데이터 연동은 로그인이 붙은 뒤부터 켠다.
- **AI 챗봇 데이터 최소화**: AI 챗봇(Anthropic Claude API)에는 답변에 꼭 필요한 최소 정보(예: 담당자 이름·연락처)만 전달하고, 사용자·장비 표 전체를 통째로 넘기지 않는다.
- API 키 관리: 렌탈사이트 API 키, Supabase(데이터베이스·인증) 키, Anthropic API 키는 모두 `.env` 파일에만 저장하고 `.gitignore`로 커밋에서 제외한다(현재 프로젝트에 이미 적용됨).

## 8. 기술 스택
*개발에 사용할 도구 모음 (이 과정에서는 Next.js로 고정)*

- 프레임워크: Next.js (App Router, TypeScript)
- 스타일링: Tailwind CSS
- 데이터베이스·인증: Supabase (Postgres + 이메일/비밀번호 인증). 자세한 이유는 DESIGN.md 4번 참고.
- 바코드 인식: @zxing/browser
- AI 챗봇: Anthropic Claude API

## 개발 단위
*핵심 기능을 만들기 좋은 작은 단위로 나눈 것, 만들 순서대로 정렬. 로그인은 일단 제외하고 진행, 마지막 단계에서 추가. (design-validator 검토를 반영해 세분화 — 최신 버전은 PLAN.md와 동일)*

1. Next.js 기본 라우팅/페이지 구조 만들기 (로그인 없이, 더미 데이터로 화면 뼈대 먼저 확인)
2. Supabase 프로젝트를 만들고 데이터 모델대로 표(users/equipment/inspections/license_requests/inspection_schedules/assignees)를 만들기
3. M365 PowerShell로 내보낸 사용자 CSV를 업로드해서 저장하는 화면·API 만들기
4. 렌탈사이트 API로 장비 데이터(배정된 실사용자 포함) 가져와 저장하기
5. 저장된 데이터로 개인 현황 대시보드 화면 만들기 (이름/사진/이메일/장비/실사·라이선스 현황)
6. 담당자용 조직 현황 화면 만들기 (팀/실 구성원 목록)
7. 카메라로 바코드를 스캔해 자산번호·실사용자 정보를 인식하는 실사 입력 화면 만들기
8. 실사 결과 제출 API 만들기 (자산번호 + 실사용자 정보 저장)
9. 실사용자 정보가 틀렸을 때 수정 후 "변경 제출"하는 흐름 추가하기
10. 관리자용 실사 현황 모니터링 화면 만들기 (진행률, 조직별·개인별 현황)
11. 실사 일정 오픈/마감 관리 화면 만들기 (생성·변경·마감·취소)
12. 라이선스 보유 리스트 조회 및 신청 화면 만들기
13. 라이선스 신청 승인/거절 처리 화면 만들기 (관리자가 확인 후 확정)
14. 라이선스 수량 관리 화면 만들기 (전체 대비 사용량, 팀별·종류별 현황)
15. 사용자 정보 바꾸기(비밀번호 변경) 화면 만들기
16. 담당자 선임 화면 만들기 (서비스 영역별 담당자 지정/변경)
17. AI 챗봇 기본 응답 만들기 (담당자·시스템 담당자 연락처 확인, 더미 데이터 기반 최소 정보만 전달)
18. AI 챗봇에 HR 요청 알림 및 자동/수동 처리 흐름 연동하기 (메일 발송은 기록만 남기고 실제 전송은 이후 과제)
19. 로그인(이메일/비밀번호) 및 사용자 권한별 접근 제어를 추가하고, 이 시점부터 실제 데이터로 전환하기
