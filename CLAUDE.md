@AGENTS.md

# itsms 프로젝트 규칙

이 문서는 `itsms` 프로젝트에서 작업할 때 지켜야 할 규칙입니다. 제품 요구사항은 [PRD.md](PRD.md)를 참고하세요.

## 프로젝트 개요
- Next.js(App Router, TypeScript) + Tailwind CSS로 만드는 사내 ITSMS(IT 서비스 관리) 웹 앱입니다.
- 소스 코드는 `src/app` 아래에 있습니다.

## 언어
- 모든 설명과 주석은 한국어로 작성한다.

## 파일 위치
- 새 파일은 `itsms` 폴더(현재 프로젝트 폴더) 안에만 만든다.

## 기술 스택
- 웹앱 기술 스택은 PRD.md에서 정한 대로 Next.js로 고정한다. 다른 프레임워크로 바꾸거나 마이그레이션을 제안하지 않는다.
- 배포는 Vercel을 사용한다.
- 예외: 외부 API 데이터 수집 배치는 `export_OA` 폴더의 Python 프로그램으로 만든다(웹앱과 분리되어 단독 실행). 웹앱 코드에는 Python을 쓰지 않는다.
- 데이터베이스는 Supabase를 사용하고, 접근은 REST API + service_role 키로 한다(Postgres 비밀번호를 쓰지 않는다).

## 작업 절차 (검증 루프)
아래 순서를 매번 반복한다.

1. 변경한다.
2. `npm run lint`와 `npm run build`를 실행해 오류가 없는지 확인한다.
3. 화면이 바뀐 작업이면 `npm run dev`로 띄워 실제로 보이는지 확인한다.
4. 문제가 있으면 고치고 다시 1)로 돌아간다.
5. 통과하면 무엇을, 왜 바꿨는지 한 줄로 요약한다.

## 비밀 정보 관리
- `.env` 등 비밀 정보 파일과 `node_modules` 폴더는 `.gitignore`에 등록해 두고, 절대 커밋하지 않는다.
- 외부 서비스 인증이 필요하면 토큰 값을 사용자에게 묻거나 채팅에 출력하지 말고, `.env`에 있는 값을 읽어서 사용한다.
  - 예: Supabase를 쓸 상황이 생기면 Supabase CLI를 설치해 `.env`의 `SUPABASE_ACCESS_TOKEN`으로 작업한다.
  - 예: Vercel 작업(배포 등)이 필요하면 Vercel CLI를 설치해 `.env`의 `VERCEL_TOKEN`으로 인증해 작업한다.

## 파일 삭제
- 파일을 지워야 할 때는 바로 삭제하지 말고, `trash-can` 폴더를 만들어 그 안으로 옮겨만 둔다. 작업이 끝난 뒤 사용자가 직접 확인하고 삭제한다.

## 서브에이전트 활용
- 이미 설치된 서브에이전트는 필요할 때마다 적극 활용한다.

## 고정된 UI 요소 (임의로 수정 금지)
- `src/app/audit_oa/ScanClient.tsx` 최상단 로고(`/audit-oa-logo.png`, `width=160 height=55`)는 사용자가 크기·위치를 확정한 요소다. 반응형이든 다른 요소가 바뀌든 이 로고의 크기·태그 값은 별도 요청 없이 임의로 바꾸지 않는다.
